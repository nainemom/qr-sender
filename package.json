{
  "name": "qr-sender",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build -- --base=/qr-sender/  && gh-pages -d ./dist"
  },
  "dependencies": {
    "history": "^5.3.0",
    "preact": "^10.11.2",
    "preact-router": "^4.1.0",
    "qr-scanner": "^1.4.1",
    "qrcode-generator": "^1.4.4"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.4.0",
    "@vitejs/plugin-basic-ssl": "^0.1.2",
    "gh-pages": "^4.0.0",
    "typescript": "^4.6.4",
    "vite": "^3.2.3"
  }
}
